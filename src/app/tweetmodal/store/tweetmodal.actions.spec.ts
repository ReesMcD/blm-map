import * as TweetmodalActions from './tweetmodal.actions';

describe('Tweetmodal', () => {
  it('should create an instance', () => {
    expect(new TweetmodalActions.LoadTweetmodals()).toBeTruthy();
  });
});
